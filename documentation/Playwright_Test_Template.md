# Playwright Test Template

## Test Identity

* Test name:
* Test ID:
* Folder:
* Pod:
* Pod URL:
* Owner:

## Goal

Describe the browser-based business outcome.

## What I Need To Run This

* Folder:
* Regression root:
* Auth method:
* Storage state available: `Yes` / `No`
* Login required: `Yes` / `No`
* Test data ready: `Yes` / `No`
* Required access or role:
* Known view / page / request / policy / location:
* Required `.env` file:
* File uploads, downloads, or attachments needed:

## Preconditions

* The pod URL is reachable.
* The browser can authenticate.
* Required test data exists.

## Steps

1. Open the pod URL.
2. Login if required.
3. Navigate to the target area.
4. Perform the action.
5. Validate the result.

## Evidence Required

* Screenshots: `Yes` / `No`
* Trace: `Yes` / `No`
* Video: `Yes` / `No`
* Run log: `Yes` / `No`
* Final summary: `Yes` / `No`

## Evidence Checkpoints

* After login
* After navigation
* Before save or submit
* After final success message
* After any failure

## Selector Guidance

* Selector policy:
* Navigation policy:
* Stop if ambiguous: `Yes` / `No`

## Notes For Codex

* Known flaky areas:
* Alternate selectors:
* Timing concerns:
* Validation rules:
